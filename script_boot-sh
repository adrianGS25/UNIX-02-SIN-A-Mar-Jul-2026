mkdir proyecto # It creates a folder
cd proyecto/ # It helps us to change to the folder proyecto/
cd /workspaces/UNIX-02-SIN-A-Mar-Jul-2026/proyecto # It helps us to change to the folder proyecto/ whit all the directory name
cd .. # move to the principal directories
pwd #  It shows in what directory you are
cd #to go to home directory
cd ~ # to go to home directory
cd $HOME # to go to home directory
cd /home/codespace $ to go to home directory
echo $BASH # Print the variable of BASH
echo $PATH # print the executable programs
ls -lai # Displays files in a long list format
ls -l -a -i # Displays files in a long-list format
man ls # Opens the manual page
ls -all # showing all files
ls -a # showing all files
cd /bin # moves you directly to the bin directory located at the root of the system.
cd .. # Moves you up one level in the folder hierarchy
. # The current directory
.. # The parent directory
stat . # is like an advanced ls 
Device: 7,4     Inode: 1573138     Links: 2 # 
device # This identifies the specific physical or virtual partition on the hard drive where the data is stored.
Inode # The Inode Index Node is like the Social Security Number of your file or folder.
Links # This tells you how many different names or paths are pointing to this exact same location on the disk. For a standard director